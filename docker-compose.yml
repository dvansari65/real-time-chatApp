services:
  postgres:
    image: postgres:16-alpine
    container_name: nextjs_postgres_db
    restart: always
    environment:
      POSTGRES_USER: dvansari65
      POSTGRES_PASSWORD: dvansari65
      POSTGRES_DB: whatsapp
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: